package graph_note;

public class SPT {
    public SPT(int source) {
        /*
        MinPQ<Double> PQ = new MinPQ<Double>();

        PQ.add(source, 0);
        for (int i = 0; i <numVertices; i++)
            PQ.add(i, MAX_VAL);
        while (!PQ.isEmpty()) {
            int p = PQ.removeSmallest();
            for (int q: G.adj(p)) {
                Relaxing(p, q);

         */
    }
    /*
    private void Relaxing(int p, int q) {
        double weight = G.getEdge(p, q);
        if distTo[p] + w < distTo[q] {
            distTo[q] = distTo[p] + w;
            edgeTo[q] = p;
            PQ.changePriority(q, distTo[q]);
        }
    }

     */
}
